/**
 * Re-exports the shared Permission enum from @farm/types for use within the
 * API application.
 *
 * The enum values are defined in the shared types package so the frontend can
 * import the same definitions without duplicating business logic.
 */
export { Permission } from "@farm/types";
